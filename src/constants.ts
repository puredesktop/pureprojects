/** Must match `plugin.json` `app.slug`. */
export const PROJECTS_APP_SLUG = 'projects'

/** App-scoped JSON store holding every project. */
export const PROJECTS_STORE_FILE = 'projects.json' as const

/** Schema version of the persisted store shape. */
export const PROJECTS_STORE_VERSION = 1
